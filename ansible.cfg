[defaults]
# Quieter, more readable output for an interactive local provision run.
inventory = inventory
roles_path = roles
stdout_callback = yaml
nocows = true
retry_files_enabled = false
interpreter_python = auto_silent

[privilege_escalation]
# Roles opt into become per-task; this just sets the method when they do.
become_method = sudo
